/** Types for the eval-patch helper, so tests can re-derive the vendored file. */
export declare const EVAL_PATCHES: [string, string][];
export declare function applyEvalPatches(source: string): string;
